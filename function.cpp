#include <string>

long double fact(int n)
{
    if(n < 0) 
        return 0; 
    if (n == 0) 
        return 1; 
    else 
        return n * fact(n - 1); 
};
long double sqr(int n, float c)
{
	return (n*n + c)
}
long double min(int n)
{
	
}